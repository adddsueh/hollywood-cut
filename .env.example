# ============================================================
# Hollywood Cut - Environment Variables Example
# ============================================================
# Copy this file to .env and fill in your values.
# NEVER commit your actual .env file to source control!
# ============================================================

# Your Google Gemini API Key
# Get one at: https://aistudio.google.com/app/apikey
# This key is entered by the user at runtime via the UI —
# it is NOT bundled into the build. This file is for
# reference only (e.g. CI/CD or server-side proxies).
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# (Optional) Static file server port — defaults to 8080
PORT=8080
